from items import Armor
from items import Weapon
from items import Potion
from other import Inv
from other import XP
from entities import Shop

test = Shop(10, 3)

print(test.toString())
print(test.getInfo(5))